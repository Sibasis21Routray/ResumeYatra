# Frontend — ResumeMaker

This is a minimal Vite + React + TypeScript starter for the ResumeMaker frontend.

Quick start (after installing dependencies):

PowerShell
```
cd frontend
npm install
npm run dev
```

Notes:
- Vite dev server runs on port 5173 by default.
- Add components into `src/` and pages under `src/pages/`.
